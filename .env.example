# Application Environment Configuration
# Copy this to .env and adjust values for your environment

# Environment: development or production
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=ithelp

# Email Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM_EMAIL=noreply@company.com
MAIL_FROM_NAME=HDesk
MAIL_ENCRYPTION=tls

# Base URL (don't include trailing slash)
BASE_URL=http://localhost/IThelp

# Error Reporting (0 = off, E_ALL = all errors)
ERROR_REPORTING=E_ALL
DISPLAY_ERRORS=1

# Notes for Production Deployment:
# 1. Set APP_ENV=production
# 2. Generate tailwind.min.css and place in assets/css/
# 3. Set DISPLAY_ERRORS=0
# 4. Update MAIL_* with production email credentials
# 5. Update BASE_URL to your production domain
