# Application Environment Configuration
# Copy this to .env and adjust values for your environment

# Environment: development or production
APP_ENV=production

# Database Configuration - Update with your hosting provider credentials
DB_HOST=localhost
DB_USER=your_database_username
DB_PASS=your_database_password
DB_NAME=your_database_name

# Email Configuration (Gmail SMTP recommended)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM_EMAIL=noreply@yourdomain.com
MAIL_FROM_NAME=HDesk
MAIL_ENCRYPTION=tls

# Base URL - Your domain (leave empty to auto-detect)
BASE_URL=

# Error Reporting (0 = off for production, E_ALL = all errors for development)
ERROR_REPORTING=0
DISPLAY_ERRORS=0

# ============================================
# PRODUCTION DEPLOYMENT INSTRUCTIONS:
# ============================================
# 1. Create a file named '.env' (not .env.example) in your root directory
# 2. Update all 'your_' placeholder values with actual credentials
# 3. Get database credentials from your hosting provider control panel
# 2. Generate tailwind.min.css and place in assets/css/
# 3. Set DISPLAY_ERRORS=0
# 4. Update MAIL_* with production email credentials
# 5. Update BASE_URL to your production domain
