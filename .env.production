# Production Environment Configuration for Hostinger
# Copy this to .env on your production server

# Environment: MUST be 'production' for production deployment
APP_ENV=production

# Database Configuration - Hostinger credentials
DB_HOST=localhost
DB_USER=u816220874_AyrgoResolveIT
DB_PASS=YOUR_DATABASE_PASSWORD_HERE
DB_NAME=u816220874_resolveit

# Email Configuration (Gmail SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=it.resourcestaff@gmail.com
MAIL_PASSWORD=fqbr ocgu jcfh jwdy
MAIL_FROM_EMAIL=it.resourcestaff@gmail.com
MAIL_FROM_NAME=HDesk
MAIL_ENCRYPTION=tls

# Base URL - Your Hostinger domain (with trailing slash)
# Leave empty to auto-detect, or set explicitly
BASE_URL=https://yourdomain.com/

# ============================================
# DEPLOYMENT CHECKLIST:
# ============================================
# 1. Upload all files to public_html/
# 2. Create this .env file with your actual credentials
# 3. Import database from database/Main DB/u816220874_resolveIT.sql
# 4. Set correct file permissions:
#    - chmod 755 for directories
#    - chmod 644 for files
#    - chmod 777 for uploads/ directory
# 5. Verify .htaccess is enabled (AllowOverride All)
# 6. Test login with admin credentials
# ============================================
