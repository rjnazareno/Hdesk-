# Dependencies
# /vendor/  # Allowing vendor for deployment
# composer.lock  # Need this for deployment

# Environment files
.env
.env.local

# IDE files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Upload files (keep directory structure, ignore uploaded files)
/uploads/profiles/*
!/uploads/profiles/.gitkeep
!/uploads/profiles/.htaccess

/uploads/tickets/*
!/uploads/tickets/.gitkeep
!/uploads/tickets/.htaccess

/uploads/temp/*
!/uploads/temp/.gitkeep
!/uploads/temp/.htaccess

# Log files
*.log
/logs/*
!/logs/.gitkeep
!/logs/.htaccess

# Temporary files
*.tmp
*.temp
*.cache

# Backup folder (old docs, debug archives)
/_backup/

# Configuration overrides (keep templates)
# config/database.php
# config/config.php
# Firebase credentials (NEVER commit these!)
config/firebase-service-account.json
config/firebase_service_account.php

# Composer
composer.phar